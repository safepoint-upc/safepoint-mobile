import 'package:flutter/material.dart';

class RiskBadge extends StatelessWidget {
  const RiskBadge({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('RiskBadge')),
    );
  }
}
